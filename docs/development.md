# Development

Branch from `release/0.8.0` under active status records and use a clean worktree when the primary checkout is dirty.
Add control-plane semantics only; migrate domain behavior to its owner and keep temporary compatibility adapters named.
Use injected clocks and storage for deterministic failure tests. Update `control_surface_manifest.json` whenever a module
maturity or migration target changes.
