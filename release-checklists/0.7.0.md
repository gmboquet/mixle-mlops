# mixle-mlops 0.7.0 release checklist

This is the first release-readiness pass this repository has gone through — there is no prior
`release-checklists/` folder, `CHANGELOG.md`, or CI configuration to build on, and the package
version has been stuck at `0.1.0` in `pyproject.toml` since the very first commit (including at the
existing `v0.6.2` git tag/branch). This file follows the same gate structure and evidence discipline
as `mixle` core's `release-checklists/0.7.0.md` and `README.md`, adapted to this repo's actual
tooling (no CI, a Docker/Helm/Terraform deploy surface).

**PyPI status (2026-07-12)**: the release owner has set up the `mixle-mlops` project on PyPI so 0.7.0
can publish there, and the docs (`README.md`, `docs/installation.rst`, `docs/quickstart.rst`,
`docs/release-notes.rst`) now describe `pip install mixle-mlops` as the standard install path ahead
of that publish. As of this pass, `pypi.org/pypi/mixle-mlops/json` and the same path on
`test.pypi.org` both 404 — no release has actually been uploaded yet (consistent with a trusted-
publisher registered ahead of the first upload, which doesn't make the project resolve via that
endpoint until a release lands). §11/Publication below stays `TODO` until an actual upload is
confirmed; don't treat the docs changes as evidence that 0.7.0 is live on PyPI.

**Status legend**: `TODO` (no evidence yet) / `PARTIAL` (evidence doesn't cover the exact release
commit or is incomplete) / `DONE` (verified against the exact commit to be released, evidence
recorded inline) / `EXCLUDED` (explicitly out of scope, one-line reason).

**Scope note** (mirrors `mixle` core's own): this tracks `mixle-mlops` itself. Cross-package
coordination (lockstep versions with `mixle`/`mixle-agent`/`mixle-knowledge`/`mixle-pde`, family
co-install, publication order) is the release owner's responsibility, tracked outside this repo.

**Merge note (2026-07-12)**: the first pass of this checklist (through commit `ff4c777`, 2026-07-11)
verified `release/0.7.0` as branched directly off `origin/main`. That pass had no visibility into a
separate, unpushed local branch (`release/0.6.3-generic-capabilities`) carrying 29 additional commits
of real, tested capability — the `pool`, `substrate`, `telemetry`, and `verbs` gateway routes, the
drift-retrain end-to-end tests, served-model certificate/calibration manifests, a full Sphinx manual,
and a new frontend runtime console — that had never been merged to `main` and so was invisible to
that first pass. This update merges that work into `release/0.7.0`, re-verifies every gate below
against the combined tip, and documents what changed as a result. Every `DONE` gate from the first
pass was re-run rather than assumed still valid.

**Feature addition (2026-07-12)**: added LoRA/QLoRA fine-tuning depth requested for this release —
SFT (chat-template + prompt masking) and `resume_from` checkpoint continuation in the generated
training script, `adapter_path` support in `HFLogitProvider`/`load_local_engine` (previously nothing
in the serving path could load a trained adapter at all), and `POST /v1/models/load` (admin-gated)
to bring a completed fine-tune live without a gateway restart. See §5 for the real, non-mocked
verification (a genuine tiny model was LoRA-fine-tuned, saved, and reloaded through the actual
serving code, both with and without the fix a first attempt needed) and `docs/gateway-capabilities.rst`
for what's deliberately out of scope (per-request multi-tenant adapter switching).

- Release branch: `release/0.7.0`
- Checklist opened: 2026-07-11; merged and re-verified: 2026-07-12; docs editorially reviewed: 2026-07-12;
  docs swept for the fine-tuning feature: 2026-07-12; ARCHITECTURE.md removed: 2026-07-12;
  `examples/` professionalism pass: 2026-07-13
- Last commit that changes test/build/import behavior: `29f4037` ("security: reject path traversal in
  every {registry_root}/{name} route"), 2026-07-12 — confirmed by diffing every commit since it against
  `mixle_mlops/` and `tests/`: the only change is one docstring line in `mixle_mlops/__init__.py`
  (repointing away from the removed `ARCHITECTURE.md`), and zero test files changed. Every commit after
  it is docs/prose-only: `cc06034`/`1fb190d` (removing `ARCHITECTURE.md`, re-verified with a full
  `pytest` run, `ruff check`, and a clean `-W` docs build) and `04b50c1` (professionalism pass over
  `examples/`, re-verified the same way — see §7). Current tip: `ce2bbc1`.

## 1. Branch and CI state

| Gate | Status | Evidence |
| --- | --- | --- |
| No open PRs against the release branch | `DONE` | `gh pr list --repo gmboquet/mixle-mlops --state open` → `[]`. The repo has never had a PR — every commit on both merged lines was pushed directly to a branch. |
| `main` is not ahead of the release branch | `DONE` | `main` fast-forwarded to this branch's tip (`6915275`, `git push origin release/0.7.0:main`) so the new CI workflows would actually go live — see the CI row below. Both `origin/main` and `origin/release/0.7.0` now point at the identical commit; `release/0.7.0` was left in place, not deleted. One thing found and deliberately left behind in this merge: a local-only, never-pushed `main` commit (`4b36955`, "set version to 0.6.2 to unify family versioning for the 0.6.3 lockstep release", 2026-07-08) — superseded by the `0.7.0` versioning decision this checklist already documents (§2), and its own parent history was already fully merged into `release/0.7.0` independently. Pushed straight from `release/0.7.0` rather than through the local `main` ref so that stale commit wasn't dragged along. |
| `v0.7.0` tag does not already exist | `DONE` | `git ls-remote --tags origin` → no tags at all exist in this repo, checked 2026-07-12. |
| CI is green on the exact current tip | `PARTIAL` (was `EXCLUDED`) | This pass added `.github/workflows/docs.yml` and `.github/workflows/publish.yml` (`fcda30e`), mirroring `mixle` core's Docs/Publish workflows (core also has `security.yml`/`tests.yml`; neither has an equivalent here yet, so §5/§6's local manual runs are still this repo's only test/security gate). `main` has since been fast-forwarded to include them (see the row above) — `gh api repos/gmboquet/mixle-mlops/actions/workflows` now lists both as `state: active`, confirming GitHub recognizes them on the default branch. Still `PARTIAL`, not `DONE`: neither has actually *executed* yet — `docs.yml` needs Pages source set to "GitHub Actions" in repo settings (still `404` from the Pages API as of this check) before a real deploy can succeed, and `publish.yml`'s real-upload path only runs on a published GitHub Release, which hasn't happened (see the Publication row). The release owner reports the PyPI Trusted Publisher for `mixle-mlops` is already registered on PyPI's side; not independently re-verified here since there's no API access to confirm it without attempting a real publish. Verified the docs build itself works with the minimal install CI would actually use: a from-scratch venv with only `pip install -e ".[docs]"` (no torch/transformers/peft) ran `sphinx-build -b html -W --keep-going` clean — every autodoc'd module defers its heavy imports. |

## 2. Version and metadata

| Gate | Status | Evidence |
| --- | --- | --- |
| `pyproject.toml` version equals the release version | `DONE` | `version = "0.7.0"` on both merged lines (resolved identically; no conflict on the value itself, only on adjacent extras — see the dependency gate below). |
| `__version__` matches package metadata | `DONE` | `mixle_mlops/__init__.py`'s `__version__ = "0.7.0"` and the FastAPI app's reported `version=` (imports `__version__`) both confirmed post-merge: fresh install → `python -c "import mixle_mlops; print(mixle_mlops.__version__)"` → `0.7.0`, checked 2026-07-12. |
| Bump is the correct semver level | `DONE` | Unchanged reasoning from the first pass — this is the first real version this package has shipped; `0.7.0` matches `mixle` core's coordinated release. |
| `requires-python` and classifiers are current | `DONE` (added) | `requires-python = ">=3.10"`, verified on 3.12 only. Added PyPI classifiers ahead of the PyPI publish: `Python :: 3`/`Python :: 3.12` only (not 3.10/3.11 — not actually tested, so not claimed), `Development Status :: 4 - Beta`, `Intended Audience :: Developers`. No OS classifier added — no CI exists to back a claim for any specific OS beyond this pass's own macOS dev environment. |
| `CHANGELOG.md` has a dated, complete `0.7.0` entry | `DONE` (expanded) | The `add/add` merge conflict between the two lines' independent `CHANGELOG.md`s was resolved by hand: kept the first pass's "Fixed" section in full, and expanded "Added" to include the merged-in capabilities (pool, substrate, verbs, telemetry routes; drift-retrain automation; model manifest certificates; the Sphinx manual; the frontend runtime console) that weren't described anywhere before this merge. |
| Migration notes for any breaking change | `EXCLUDED` | No prior published release exists to migrate from. |
| `mixle` dependency floor — realistic given `mixle` core's current 0.7.0 state? | `DONE` | Unchanged from the first pass (`mixle>=0.6.1`, evidenced against all 4 published `mixle` releases) — the merge didn't touch this dependency or add any new top-level `mixle.*` import that isn't already covered by that floor (checked via the import-AST sweep in §4). |

## 3. Build and install clean

| Gate | Status | Evidence |
| --- | --- | --- |
| `python -m build` produces sdist + wheel with zero errors | `DONE` | Rebuilt from `f3b830f` (the merged tip, not the stale first-pass `d48823e`): `mixle_mlops-0.7.0.tar.gz` and `mixle_mlops-0.7.0-py3-none-any.whl`, zero errors, checked 2026-07-12. |
| `twine check dist/*` | `DONE` | Both artifacts → `PASSED`, checked against the rebuilt `f3b830f` wheel/sdist. |
| Fresh, isolated venv install (not editable, not `PYTHONPATH`) | `DONE` | Fresh `python3.12 -m venv`, `pip install` the rebuilt wheel directly, no extras, no editable, no `PYTHONPATH`, run from `/tmp` (no local `mixle`/`mixle_mlops` tree to shadow the install). |
| Import smoke from that install | `DONE` | `import mixle_mlops, mixle; print(...)` → `0.7.0 0.7.0`; `from mixle_mlops.gateway import create_app; create_app()` → constructs cleanly, **28 routes** (up from the first pass's 24 — the 4 new route modules: `pool`, `substrate`, `telemetry`, `verbs`). |
| Full public-module import sweep | `DONE` | Bare install (no extras): **123/123 modules** import cleanly, 0 failures (up from 119 — the merged route/support modules). |
| Non-`.py` package data present in the wheel | `DONE` (resolved) | `unzip -l`: `mixle_mlops-0.7.0.dist-info/{METADATA,WHEEL,entry_points.txt,top_level.txt,RECORD,licenses/LICENSE}`. The first pass's open finding — no `LICENSE` file / no copyright-holder decision — is resolved: the merged-in `LICENSE` carries `Copyright (c) 2026 Grant Boquet` (a personal, not institutional, notice), and it is now bundled in the wheel. |
| End-to-end functional smoke (beyond import) | `DONE` (re-verified + expanded) | `mixle-mlops init-db` + `create-user ... --admin` against a fresh sqlite DB, then a real out-of-process `mixle-serve` hit over HTTP: `GET /health` → `200 {"status":"ok","service":"mixle-mlops"}`; `GET /v1/models` → real model list, including a live `certificate` field on `demo-mixle` (the new manifest-certificate feature) and `null` on models that can't support one; `GET /v1/pool/spend` → `{"spent":0.0,"quota":100.0,"remaining":100.0,...}`; `GET /v1/telemetry/stats` → `{"n_events":0,"kinds":{}}`. The last two specifically exercise routes that only exist after this merge. |

## 4. Dependency correctness

| Gate | Status | Evidence |
| --- | --- | --- |
| Declared deps match imports (both directions) | `DONE` | Re-verified post-merge via the full `[all,dev]` test run (§5) and the 123/123 bare-install import sweep (§3) — no new undeclared top-level import surfaced from the merged-in route modules. The first pass's fixes (`numpy`/`scipy`/`diffusers` declared, `image` extra added) all carried through the merge untouched. |
| Dependency graph resolves clean | `DONE` | `pip install --dry-run '.[all]'` → resolves clean against the merged `pyproject.toml`, checked 2026-07-12. |
| Version bounds are real, not just present | `DONE` | Re-verified against the merged tip specifically because this is the highest-risk regression point: installed `transformers==5.13.1` (the exact version that broke `TreeLogitProvider` pre-fix) into the `[all,dev]` venv and ran the full suite — **328 passed, 1 skipped, 0 failed**, confirming the first pass's `providers.py` fix survived the merge (it merged cleanly with no conflict, since the previously-unmerged branch never touched that file). |
| Optional deps stay truly optional | `DONE` | Confirmed via the same 123/123 bare-install sweep (no extras installed, 0 failures). |
| The `mixle` dependency itself actually installs and imports correctly at the declared floor | `DONE` | Unchanged from the first pass; not affected by the merge. |
| `pyproject.toml`'s optional-dependency table itself | `DONE` (merge conflict resolved) | Both lines had independently added an extra in the same list: the previously-unmerged branch added `docs = ["sphinx>=7", "furo"]` (needed for its Sphinx manual); the first pass added `image = ["diffusers>=0.27", "torch>=2"]` (the `diffusers` fix). Resolved to keep both, and set `all` to bundle every *runtime* extra (`documents,scale,export,datasets,structured,local,cloud,mcp,image`) while deliberately excluding the build-time-only `docs` extra. |
| Platform-incompatible dependency spec | `EXCLUDED` | Unchanged: no platform-conditional dependencies declared. |

## 5. Test rigor

| Gate | Status | Evidence |
| --- | --- | --- |
| `ruff check mixle_mlops tests` | `DONE` | Clean: `All checks passed!` against `bbfbfdd`, checked 2026-07-12. |
| `ruff format --check` | `PARTIAL` | Unchanged finding, updated count: 144/184 files would be reformatted (was 141/182 — the 3 new test files this pass added are themselves ruff-format-clean; the ratio is unchanged). Never this repo's documented convention; not re-fixed here for the same reason as prior passes. |
| Full test suite, base + `dev` extras (`pip install -e ".[dev]"`) | `DONE` | Against `bbfbfdd`, torch/transformers/peft genuinely absent (confirmed): **302 passed, 11 skipped, 0 failed** (was 295/10 before the LoRA/SFT/CPT feature pass — the +7 are `test_models_load.py`'s 5 route tests and `test_compute.py`'s 2 new string-level tests, neither needs `local` extras; the +1 skip is `test_local_engine.py`'s single module-level `importorskip` on `torch`). **Superseded** by the traversal fix (`29f4037`, landed after `bbfbfdd`), which added its own tests on top of this count — see the current total in §6's path-traversal row (363 passed / 3 skipped) rather than this figure alone; not re-split by tier here since zero test files have changed since. |
| Full test suite, `[all,dev]` extras (torch, transformers, peft, pandas, pyarrow, reportlab, jsonschema, mcp, redis, psycopg, boto3, s3fs/gcsfs/adlfs/ossfs, diffusers) | `DONE` | Against `bbfbfdd`, `transformers==5.13.1`: **338 passed, 1 skipped, 0 failed** (was 328/1 — the +10 are the 3 real adapter-loading tests in `test_local_engine.py` plus the +7 above). **Superseded** the same way as the row above — see §6's 363/3 for the current total; no test file has changed since either row's capture, so nothing here is stale in substance, only in which single number is the final one to cite. |
| Integration / API-contract coverage (this is a serving gateway) | `DONE` (expanded) | The first pass's `TestClient`-driven route suites, plus the newly-merged `tests/test_pool_serving.py`, `tests/test_substrate_serving.py`, `tests/test_telemetry_serving.py`, `tests/test_verbs_serving.py`, and the drift-retrain end-to-end tests in `tests/test_drift_retrain.py` — all included in the §5 pass counts above. Supplemented with the real out-of-process smoke in §3 hitting the two newly-merged routes specifically (`/v1/pool/spend`, `/v1/telemetry/stats`). |
| New capability: LoRA/QLoRA adapter serving, SFT, `resume_from` | `DONE` | Committed tests (`test_local_engine.py`, `test_models_load.py`, `test_compute.py`) cover the adapter-loading mechanism (real in-process tiny GPT-2, no download) and the route/script-generation logic, per §5's committed-suite counts. Beyond that, ran a real, uncommitted, ad-hoc end-to-end check: a genuine tiny GPT-2 + a hand-set chat template (`hf-internal-testing/tiny-random-gpt2`, network-fetched) was LoRA-fine-tuned by the actual generated script on real `messages`-format data, produced a real `adapter_config.json`/`adapter_model.safetensors`, and was reloaded through the actual `HFLogitProvider(adapter_path=...)` path — confirmed `type(model).__name__ == "PeftModelForCausalLM"`, not a silently-skipped adapter. This real run caught a genuine bug a string-only test would have missed: `apply_chat_template(tokenize=True)` returns a dict (not a plain token list) in this `transformers` version unless `return_dict=False` is passed explicitly — fixed (`bbfbfdd`) before landing. Not part of the committed suite: it needs network access to a public HF test model, which conflicts with this repo's established no-network-dependency convention for model tests (`test_tree_provider.py` builds its tiny model from scratch for the same reason). |
| Known non-blocking flake | `DONE` | Unchanged — the `SeekIndex` tie-break fix (`ff4c777`) merged cleanly and both transformers versions still pass 6/6 in `test_tree_provider.py`. |
| Supported OS/Python matrix stated and covered | `PARTIAL` | Unchanged: verified 3.12 only. |

## 6. Hygiene scan

| Gate | Status | Evidence |
| --- | --- | --- |
| No secrets in code/tests/docs | `DONE` | Re-scanned the full merged tree (credential-shaped strings: `sk-...`, `AKIA...`, `ghp_...`, PEM headers) across `mixle_mlops/`, `deploy/`, `k8s/`, `frontend/`, `docs/`, `Dockerfile`s: zero matches, checked 2026-07-12. |
| Default credentials / secrets (gateway-specific) | `DONE` | Unchanged — the first pass's `check_secret_key()` guard merged cleanly (no conflict) and is exercised by the existing account/config tests in the §5 pass count. |
| No debug leftovers (`breakpoint()`, `pdb.set_trace()`) | `DONE` | Re-scanned `mixle_mlops/` and `tests/` post-merge: zero matches. |
| Author/committer identities are acceptable | `DONE` | `git log --format='%an <%ae>' origin/main..HEAD \| sort -u` → only `Grant Boquet <grant.boquet@gmail.com>` across all 49 commits on the merged branch. |
| Path traversal via a client-supplied name used as a filesystem path segment | `DONE` (found and fixed, `29f4037`) | Noticed while reviewing the new `POST /v1/models/load`: `root / body.name` with no validation, and `substrate.py`'s `_shard()` had the identical pre-existing pattern (most of its routes are `require_user` only, not admin). Grepped every route module for the same `<root> / <user-supplied-name>` shape and found four more: `tasks.py`, `route.py` (a direct `write_text` with no existence check first — the most exposed of the six), `solutions.py`, `agentic.py`. Added a shared `gateway/routes/_paths.py` (substring reject on empty/`/`/`\`/`..`, plus an independent `.resolve()`-based check) and applied it at all ~13 call sites. Confirmed `pool.py`/`verbs.py`/`telemetry.py` are **not** vulnerable — they key off the authenticated user's own server-generated id, not client-supplied text (`pool`/`verbs` even already `.replace("/", "_")` it), and `pool`'s job-id route checks the id against the user's own stored job list before ever touching the filesystem. Verified empirically (not assumed) that a forward-slash/`..` payload in a URL *path parameter* gets resolved or blocked by the client/routing layer before reaching the handler — backslash survives routing intact and is what the four path-parameter routes' tests actually use; `models.py`'s `name` comes from the JSON body instead, so real `../`-style traversal was tested directly there. `route.py` had no existing test file at all; added a minimal one. 33 new/changed tests, full suite 363 passed / 3 skipped, ruff clean. **Currency check at `ce2bbc1`** (the actual current tip, three commits later): confirmed via diff that no test file changed since this row's commit, so 363/3 is still the accurate full-suite count; additionally re-ran the suite in a venv with torch/transformers/peft present (not the exact `[all,dev]` set — diffusers/pandas/pyarrow/reportlab/jsonschema/mcp/redis/psycopg/boto3/s3fs family absent) as a lighter freshness check on the `examples/` prose commit specifically: **351 passed, 4 skipped, 0 failed**, `ruff check` clean, all 11 example scripts `py_compile` clean. The lower passed-count and higher skip-count versus 363/3 reflects that narrower package set, not a regression — no test was removed or newly failing. |

## 7. Documentation

| Gate | Status | Evidence |
| --- | --- | --- |
| `README.md` reflects current version/capabilities | `DONE` | First pass's fixes (stale self-evolution caveat, extras list) carried through the merge without conflict. |
| `CONTRIBUTING.md` / `SECURITY.md` present | `EXCLUDED` | Unchanged — neither exists. Flagged again in Findings. |
| Docs build (Sphinx) | `DONE` (was `EXCLUDED` — no longer applies) | The first pass marked this `EXCLUDED` because no docs tooling existed on `origin/main` at the time. The merge brought in a full Sphinx manual (built by the previously-unmerged branch): **16 narrative pages** (corrected from an earlier, uncounted "43" estimate in this file's prior revision) plus generated API reference for every public module. Built with `sphinx -b html -W --keep-going`: **first attempt failed** — one warning-as-error, `autodoc: failed to import 'app' from module 'mixle_mlops'`, because `origin/release/0.7.0`'s removal of the vestigial `mixle_mlops/app.py` (a real, good fix) left the docs tree (built before that removal existed) with a stale autodoc page and two narrative references pointing at the now-deleted module. Fixed (`207d928`, `f3b830f`): dropped the stale `docs/api/mixle_mlops.app.rst` page and its toctree entry, repointed `api-overview.rst` at the real `mixle_mlops.gateway.app`, and replaced quickstart's "serve via `mixle_mlops.app`" example with the actual current path (the gateway's `/v1/mixle/*` routes). Rebuilt clean: **build succeeded, 0 warnings**. |
| Full editorial review of every narrative page (not just a clean build) | `DONE` (6 real issues found and fixed, `c75aee9`) | A clean `-W` build only proves autodoc/cross-references resolve, not that the prose is accurate or complete — read all 16 narrative pages plus a sample of the generated API reference in a rendered browser. Found and fixed: (1) `quickstart.rst` and `operator-runbook.rst` told a reader to install the `docs` extra (Sphinx tooling) to run the gateway, copy-pasted from a nearby block — corrected to `dev`. (2) `installation.rst`'s optional-extras command was missing the new `image` extra. (3) `gateway-operating-contract.rst`'s route-families table and user-scoped-data list covered pool/telemetry but not substrate or the creation-verb/skills/lineage routes — this contradicts this file's own prior-revision claim (now corrected) that this page was "found consistent" with the merged-in routes; it was not, until this pass. (4) `release-notes.rst` and `changelog.rst` (the manual's own, distinct from the root `CHANGELOG.md`) still described 0.6.3 as current, including a "Known Risks" list this same 0.7.0 pass had already resolved or narrowed — added 0.7.0 entries and corrected the risk list. |
| `linkcheck` (not just `-W` build) | `DONE` (2 broken links found and fixed, `c75aee9`) | Ran `sphinx -b linkcheck` — not part of the first pass's gates. Found 2 real broken links: `changelog.rst` linked the root `CHANGELOG.md` at `github.com/.../blob/main/...`, which 404s because that file only exists on this release branch, not yet on `main` — repointed at the release branch with a note to move it once merged. `mixle_mlops/compute/vast.py`'s module docstring rendered its Vast.ai API base URL as a bare, autolinked link that 404s for a human (it's a REST base path, not a browsable page, and the code's actual `API` constant is correct and untouched) — reformatted as a code span so it stops being autolinked. Re-ran `linkcheck` clean (0 broken) after both fixes. |
| Documented API examples actually work | `DONE` | Re-verified post-fix: quickstart's gateway-start command (`python -m uvicorn mixle_mlops.gateway.app:app ...`) matches the real module path; the corrected "serve a packaged model" text matches the real `/v1/mixle/*` routes exercised in §3's live smoke. |
| Process references resolve within this repo | `DONE` | Re-checked post-merge and post-fix; no other broken internal doc reference found. |
| `examples/` scripts are professional and internally consistent | `DONE` (`04b50c1`) | Read all 11 tracked scripts (plus the `mixle_neural_experts` README) end to end. Found and fixed two issues: (1) a personal name ("Grant's thesis") in `adapt_vlm_structured.py`'s module docstring, replaced with "The core claim" — the one place first-person authorial voice had leaked into shipped example code; (2) an ALL-CAPS-for-emphasis prose habit (e.g. `REAL`, `NEVER`, `HONEST`, `RELIABLY`) scattered across docstrings, comments, and `print()` output in six scripts, inconsistent with the plain-prose style already enforced in `mixle_mlops/*.py` docstrings — toned to normal sentence case throughout. Left untouched: legitimate acronyms/identifiers (`VLM`, `CLIP`, `CIFAR`, `SFT`, `CACHE`, `OUT`, etc.) and `platform_tour.py`'s `"TOUR COMPLETE"` banner, which the docstring explicitly documents as the literal smoke-test marker to look for (a functional convention, not stylistic emphasis). Verified with a full-repo grep sweep for remaining bare `[A-Z]{3,}` tokens (excluding known acronyms) after the edits, confirmed all 11 scripts still `py_compile` clean, `ruff check examples/` clean, and the full suite green (351 passed, 4 skipped). This is a prose/quality review, distinct from the still-open execution-manifest gap below (running and classifying each script pass/fail), which remains unresolved. |
| `ARCHITECTURE.md` reflects current state | `EXCLUDED` (removed, `cc06034`/`1fb190d`) | This gate's original `DONE` claim ("no stale references found") was wrong — see the Findings entry below, which caught real, extensive staleness this row missed. Superseded: the release owner confirmed the file was internal planning that was never meant to be public, so it's removed from the repository rather than rewritten. References fixed at the same time: `README.md`'s "Design" section and `mixle_mlops/__init__.py`'s module docstring both pointed to it; both now point at `docs/overview.rst`/`docs/package-map.rst` instead. |
| Catalog/narrative docs describe the merged-in capabilities | `DONE` (corrected) | `docs/gateway-capabilities.rst` documented the pool/substrate/verbs/telemetry surface in full prior to this merge. `docs/gateway-operating-contract.rst` did **not** — see the editorial-review row above; now fixed. |
| Manual has a favicon, a visible version, and family visual continuity | `DONE` (was missing entirely) | The manual had no favicon (`html_favicon` unset), no version anywhere in the title, sidebar, or elsewhere, and no visual connection to `mixle` core's docs, which use a shared, wordmark-free icon (`mixle_icon.png`) for exactly this kind of family consistency. Added the same icon as this manual's favicon (safe to reuse: it carries no "mixle" text, unlike the sidebar logo, which does and was deliberately *not* copied here since that would misrepresent these docs as core `mixle`'s) and restored the version in `html_title` (`mixle-mlops 0.7.0`, confirmed in both the browser tab title and the sidebar). Did not apply core's brand color CSS variables — a real design decision left for the release owner, not decided unilaterally here. |

## 8. Post-merge and final re-verification

| Gate | Status | Evidence |
| --- | --- | --- |
| Full suite green against the exact tip that will sit on the release branch | `DONE` | Every gate above was re-run against `f3b830f` — the actual final tip — not assumed from either pre-merge branch's stale evidence. |
| Re-run after every commit that landed during this pass | `DONE` | Commit sequence this pass: merge commit (`f60de45`) → docs stale-reference fix (`207d928`, `f3b830f`). Full test suite re-run after the merge and again after the docs fixes; both green, identical pass counts. |
| Merge conflict resolution reviewed line-by-line, not auto-resolved | `DONE` | Two real conflicts (`CHANGELOG.md`: add/add; `pyproject.toml`: version string + adjacent optional-dependency entries) resolved by hand — see §2/§4 evidence. Everything else merged automatically with no conflict, including the highest-risk file (`engines/providers.py`, the `transformers`-version fix), which the two lines never both touched. |
| CI green against the exact tip | `EXCLUDED` | Same reason as §1's now-`PARTIAL` finding: `docs.yml`/`publish.yml` are live on `main` now but have never actually run (no docs push has landed since, no Release has been published), and there is still no test-running workflow to be green against. §5's local runs stand in for this gate as before. |

## 9. Reproducibility

| Gate | Status | Evidence |
| --- | --- | --- |
| Resolved dependency set captured for this release | `DONE` | Regenerated against the merged tip: [`0.7.0-freeze-base.txt`](0.7.0-freeze-base.txt) (36 packages, bare install) and [`0.7.0-freeze-all.txt`](0.7.0-freeze-all.txt) (152 packages, `.[all,dev]`) — both superseding the pre-merge captures. |
| The *previous* release still installs and smoke-runs | `EXCLUDED` | Unchanged — no previous release exists. |

## 10. Rollback / incident readiness

| Gate | Status | Evidence |
| --- | --- | --- |
| The rollback path is understood before publishing | `DONE`, documented | Unchanged from the first pass. |
| Bad-deploy rollback, operationally (this is a *serving* system) | `DONE`, documented | Unchanged from the first pass; the merged-in `promote`/lineage (`verbs` route) and drift-retrain reference-rollforward give this *more* operational rollback surface than the first pass described, not less — `POST /v1/create` + `GET /v1/lineage/{model_id}` make "what does this artifact depend on, and what depends on it" a queryable check before rolling a model back. |

## Out of scope (per this pass's brief)

| Section | Status | Reason |
| --- | --- | --- |
| §11 equivalent — Publication (build+upload to any index, any "Publish" workflow, container registry push) | `DONE` | Published at the release owner's explicit direction, 2026-07-13. Tagged `v0.7.0` (annotated, at `ffe654a`) and published a GitHub Release (`gh release create v0.7.0`, notes drawn from `CHANGELOG.md`'s `[0.7.0]` section) — this triggered `publish.yml` run [29224365569](https://github.com/gmboquet/mixle-mlops/actions/runs/29224365569): build, `twine check`, and the PyPI Trusted Publishing upload all succeeded. Confirmed directly against `pypi.org/pypi/mixle-mlops/json`: `version: "0.7.0"`, `releases: ["0.7.0"]`. Also re-verified the exact artifact locally before tagging: fresh `python -m build` + `twine check dist/*` from the precise commit to be tagged, both clean. Side effect worth flagging: the same `main` push that made `publish.yml` live also matched `docs.yml`'s path filter, so it fired too — the build step succeeded but the Pages deploy step failed (`404`, "Ensure GitHub Pages has been enabled"), since Pages source has never been set to "GitHub Actions" in repo settings. Not fixed here — that's a repo-settings change, left for the release owner; the Docs workflow will succeed on the next push once Pages is enabled (or a manual `workflow_dispatch` re-run after enabling it). |
| §13 equivalent — Sign-off | `TODO` | Reserved for the human release owner. Every gate above is now `DONE`, `PARTIAL` (understood), or `EXCLUDED`, and the release itself has shipped (see the row above) — sign-off is the release owner's own final confirmation, not something to presume here. |

## Findings for the release owner (not fixed in this pass — judgment calls, not "bugs")

- **`ARCHITECTURE.md` — resolved, not just flagged.** It described an early 7-phase build plan and a
  much smaller module set (`monitoring/` instead of the actual route/service breakdown) — no mention
  anywhere of `compute`/`training` (LoRA/QLoRA), `evolve`, pool, substrate, telemetry, verbs, drift
  retraining, RAG, MCP, or the frontend runtime console. Raised to the release owner, who confirmed it
  was internal planning never meant to ship publicly. Removed from the repository rather than
  rewritten (a rewrite would have been the wrong call anyway — this file predates nearly the whole
  current feature set, so "updating" it would mean writing a new architecture doc from scratch, not
  editing the old one). `README.md` and `mixle_mlops/__init__.py`'s references to it were repointed at
  `docs/overview.rst`/`docs/package-map.rst`.
- **The core finding of this pass**: 29 commits of real, tested capability (pool/substrate/verbs/
  telemetry routes, drift-retrain end-to-end tests, model manifest certificates) sat on a local-only
  branch that was never pushed to `origin` or merged to `main`, invisible to the first release-prep
  pass. Worth a process fix going forward — e.g. pushing feature branches even before they're ready
  to merge, so a release-prep pass run from a fresh clone can at least discover them exist.
- **No CI configured at all, until this pass** (§1/§8) — a `docs.yml` and `publish.yml` were added
  (`fcda30e`), mirroring `mixle` core's workflows, but there is still no `tests.yml`/`security.yml`
  equivalent: every test/build/security gate in this file is still a local, manual, one-time
  verification, not something CI re-runs on the next commit. Worth adding next, given how much of
  this checklist's effort each pass goes toward re-proving gates a real test workflow would keep green
  automatically.
- **`docs/example-execution-manifest.rst` states its own requirement has not been met**: every one of
  the 11 example scripts under `examples/` must be recorded `passed`/`failed`/`timed_out`/`blocked`/
  `skipped` before public release, and none of this pass's verification work actually ran or
  classified them (several need GPU or live provider credentials this environment doesn't have).
  This is a real, currently-true gap the manual itself flags — not fixed here because it needs actual
  execution, not a documentation edit.
- **No `CONTRIBUTING.md`/`SECURITY.md`** (§7), unchanged from the first pass.
- **`ruff format --check` would reformat 141/182 files** (§5) — unchanged finding, updated count.
- **Cross-package floor coordination**: unchanged from the first pass — this package depends only on
  `mixle` core today.
- **`ruff format --check` is still not part of this repo's documented lint gate** even though the
  merged-in Sphinx docs pass and this pass's own commits were both written compatibly with it;
  standing up `ruff format --check` in CI (once CI exists at all) would prevent the gap from growing
  further before someone eventually pays down the existing 141 files.
